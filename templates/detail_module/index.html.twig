{% extends 'base.html.twig' %}

{% block title %}Hello DetailModuleController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
       <table>
    <thead>
        <tr>
            <th colspan="3">Detail module</th>
        </tr>
    </thead>
    <tbody>  
        <td>Nom</td>
        <td>Description</td>
        <td>Number heure</td>
                        
        <tr>
            <td>{{module.nom}}</td>
            <td>{{module.description}}</td>
            <td>{{module.numHeure}}</td>            
        </tr> 
    </tbody>
    </table>

     <p class="title">Cette module présente dans formations suivent</p>
            <ul> 
               {% for el in module.formation%}
                    <li><a href="{{ path('detail_formation', {'id':el.id}) }}">{{el.nom}}</a></li>
                {% endfor %}  
            </ul>   
              
       
</div>
{% endblock %}
