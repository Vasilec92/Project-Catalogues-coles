{% extends 'base.html.twig' %}

{% block title %}Ecole{% endblock %}

{% block body %}
    <h1>Ecole</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ecole.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ecole.nom }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ ecole.adress }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ ecole.ville }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ ecole.photo }}</td>
            </tr>
            <tr>
                <th>Formation</th>
                <td>
                    <ul>
                    {% for el in ecole.formation%}
                    <li>{{el.nom}}</li>
                        
                    {% endfor %}
                    </ul>
                    {# <a href="{{ path('addformation', {'id': ecole.id}) }}">Add formation</a> #}
                </td> >
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ecole_index') }}">back to list</a>

    <a href="{{ path('ecole_edit', {'id': ecole.id}) }}">edit</a>

    {{ include('ecole/_delete_form.html.twig') }}
{% endblock %}
